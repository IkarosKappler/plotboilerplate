/**
 * @author  Ikaros Kappler
 * @version 1.0.0
 * @date    2021-12-17
 */

globalThis.findLongestPathInGraph = (function () {
  /**
   *
   * @param {vertices: Vertex[], egdes: {i:number; j:number}[] } graph - The graph to process.
   */
  var lp = function (graph) {
    // OK. Just find the longest path in graph.
  };

  return lp;
})();
