/**
 * An experimental top class to wrap demo environments.
 *
 * @date    2026-04-13
 * @author  Ikaros Kappler
 * @version 1.0.0
 */

(function (_context) {
  var AppContext = function (pb, config) {
    this.pb = pb;
    this.config = config;
  };

  _context.AppContext = AppContext;
})(globalThis);
